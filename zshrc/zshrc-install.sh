# install zsh
sudo apt install zsh

# install starship
curl -sS https://starship.rs/install.sh | sh

# install oh-my-posh to ~/bin or ~/.local/bin
# curl -s https://ohmyposh.dev/install.sh | bash -s

# add the theme to zsh
# oh-my-posh init zsh --config ~/jandedobbeleer.omp.json
